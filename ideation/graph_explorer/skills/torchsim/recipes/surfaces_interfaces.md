# Recipe: surfaces and interfaces

Build slab/interface with adequate lateral size/vacuum -> relax positions without relaxing vacuum unintentionally -> NVT equilibration -> analyze surface reconstruction, layer density, interfacial mixing, adhesion, stress -> render with cell and normal.
